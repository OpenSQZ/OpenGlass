#pragma once

// Sanitized documentation example only.
// This file is not yet wired into the current tracked firmware include structure.
// Copy values into a local, ignored configuration file before experimenting.

#define OPENGRASS_WIFI_SSID "YOUR_WIFI_SSID"
#define OPENGRASS_WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
